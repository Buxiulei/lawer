# 运维清单（manager 持有）

## 密钥与备份（丢失即灾难级）
- [ ] `LAWER_DATA_KEY`（字段级加密主密钥）：**丢失=所有 *_enc 加密列永久不可读**。异地备份两份（密码管理器 + 离线介质），部署新环境时从备份注入，绝不重新生成。
- [ ] `JWT_SECRET`：丢失仅导致全员重新登录，仍需备份。
- [ ] SQLite 数据库 + /data/files 文件库：每日加密备份异地（backup.sh，M2 交付）。
- [ ] 凭据清单（值不入库，仅登记项名与来源）：模型 key（OpenAI/DeepSeek/DashScope 取自 NBDpsy；**Anthropic 待用户提供**）、阿里云短信/实人认证、SMTP、微信支付/服务号、支付宝证书。

## 🔒 已在主干但**未放行上线**的改动（滚更前必看）

> **规矩**：主干上有代码 ≠ 允许上线。下表未清空之前，**滚更必须逐条确认放行条件已满足**；
> 条件未满足的，**回退该改动后再滚更**，不许"顺手一起带上去"。
> **写在这里而不是只写在教训册里，是因为做滚更的人不会去翻教训册。**

| 改动 | SHA | 为什么押后 | 放行条件（全部满足才放） |
|---|---|---|---|
| 杠杆闸**第二层**（裸内疚短语须与"离开为前提"同现） | `a6a23bb` | **行为面改动且 L1 相关**（改的是危机轮哪些句子被剥，进而决定会不会级联到整段回落） | ~~① 全转录回放~~ **已跑，见下**<br>**② S08 两跑成绩单 ← 仅剩这一条，且现在它是唯一承重的证据** |

> **① 的结论（`324bd8b`，2026-08-25）：跑了，但这批语料对这道闸没有判别力。**
> 145 轮真实转录在第二层前后 **145/145 逐字相同，两版全部 `clean`——闸一次都没开过火**。
> 原因：**归档正文是闸后产物，会被剥的句子早就不在里面，回放只能看幸存者。**
> （探针已自证不是死的：同一处置链喂已知杠杆句产出 stripped / fallback。）
>
> ⚠️ **所以不许把 ① 读成"已验证"而放宽 ②。** 恰恰相反：
> **① 交白卷这件事，把 ② 从"再确认一次"变成了"唯一的证据"。**
> **一个做了但没有判别力的验证，不减轻后续验证的责任——它只是把责任原样传下去了。**

**回放结果的读法**（**在拿到结果之前就定死，防事后挑一个讲得通的说法**）：
- **零差异** ⇒ 写成「**这批转录对该修法没有判别力**」，**不许写成"验证通过"**；
- **有差异** ⇒ **逐条列**：轮号 / 被剥原句 / 修前修后处置（stripped·fallback·clean）。
  其中**修前 fallback、修后不 fallback** 的那几轮，才是这个修法的价值证据。

> ~~**为什么第二层可以等**：**第一层（来源判别，`c009c1c`，已上线）已堵住最坏路径——整段回复被吞掉**。~~
>
> ⚠️ **2026-08-26 更正（manager 裁定，WS2 查实）：这段论证的前提是假的。`c009c1c` 从未上线**
> ——线上 `00ce720` 与 `origin/main` 的 `crisis.ts` **逐字节相同**（`wc -l` 各 565 行；ws/backend 为 672 行），
> 两者 `userSaid` 出现 **0** 次、`stripLeverageWithTrail` **0** 次、`GUILT_PHRASE` **0** 次，
> 签名是 `detectEmotionalLeverage(text: string)` 直接 `EMOTIONAL_LEVERAGE_RE.exec(text)`；
> `c009c1c` / `2f32321` / `a6a23bb` 三个 `merge-base --is-ancestor origin/main` **全部为否**。
>
> **所以「最坏路径已被堵住」不成立——整段回复被吞掉这条路径，此刻在线上完全敞着。**
> 「分两层、危险的那半先走」这个设计**仍然正确，但它没有被执行**：两层谁都没走。
> 第二层的放行闸（S08 两跑）**继续有效且不放宽**；变的不是闸，是**闸外的风险底数**。
>
> **不单独抢发第一层**（manager 裁定二）：`c009c1c` 是行为面改动且 L1 相关，
> 它从来没有过自己的闸，而当初没给它设闸的**唯一理由就是「以为它已经上线了」**——那个理由现在没了。
> **在一个假前提上豁免过的东西，前提塌了就要退回未验证态，不能顺势当成已验证。**
> 它的证据与第二层同批取得：S08 两跑的 A/B/C 三版离线对照里，**B vs A 就是第一层的独立效果**。

**这次踩的是同一条通则**（manager 2026-08-26 入册）：
> **「已上线」是一个可验证的断言，必须用线上产物的内容比对来证明；
> 不许用拓扑（分支名、合并记录、PR 状态）或文档里的陈述代替。**
> 拓扑回答的是「这个 commit 在不在那条线上」，文档回答的是「有人曾经这么写过」，
> **两者都不回答「跑在用户面前的那份代码里有没有这段逻辑」**。
> 今天两次踩它：用死 ref 判分支合并（拓扑），本文件用一句陈述判第一层上线（文档）。

## 上线前人工核验（AI 无法替代的两通电话）
- [ ] 拨打验证：北京市心理援助热线（回龙观）800-810-1117 / 010-82951332 是否仍 24 小时（资源卡口径）。
- [ ] 拨打验证：北京市总工会职工法律服务中心现址电话（资源卡暂只写 12351 转接）。

## 合规监测（小型个人信息处理者豁免的持续证据，C05 §9）
- [ ] 每月核查两数：注册用户总数（近 10 万提前切回完整义务）；境外模型触达用户累计数（SQL：token_usage 中 anthropic/openai 模型的 distinct user 数——豁免线为不满 10 万人且不含敏感信息，脱敏前置是承重墙）。无需新埋点，管理端出只读报表即可（M3）。

## 恢复演练
- [ ] M2 结束前做一次全量恢复演练（新目录起服务→注册→上传→固化→验证页全通）。
