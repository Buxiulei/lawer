# 契约提案 · 案件里程碑模型（批 6 驾驶舱用）

- 提案人：WS3 前端页面 ｜ 日期：2026-08-28 ｜ 状态：**待 manager 裁定**
- 收件：manager（裁定）、后台技术（落地）
- 依据：《土八鼠产品方案》叁「驾驶舱的三个部件」、批 6 派单
- 现状底稿：`app/src/app/_mock/types.ts`、`app/src/app/_mock/demo.ts`（逐行读过，非二手摘要）

## 一、问题

驾驶舱要渲染「五阶段里程碑时间轴，完成/进行中/未到 三态，**允许回退**」。
现有类型里**没有里程碑模型**。有两个相邻的东西，都不够：

| 现有 | 是什么 | 为什么不够 |
|---|---|---|
| `CaseRecord.stage`（12 值枚举） | 单值**当前态** | 没有历史。只知道现在在哪，不知道哪些走过、什么时候走过 |
| `TimelineEvent`（事件日志） | 有历史、有时间戳 | **没有阶段归属**，也没有三态。它是流水账，不是里程碑 |

## 二、核心结论：三态不能从 `stage` 标量推

这条是「回退」那个要求逼出来的，不是风格偏好：

产品方案写「允许回退（谈崩重回仲裁不算倒退，**时间轴如实记**）」。
若三态 ＝ 拿 `stage` 的序号和阶段序号比大小，那么 stage 从「已立案」退回「仲裁准备」的那一刻，
「立案」会从 `完成` 翻回 `未到` —— **立案那天确实发生过这件事，这个事实被抹掉了**。
这正是「如实记」的反面，也正是产品方案第贰条骂线性进度条失真的同一个毛病。

→ **三态必须从只追加的事件流推**：某里程碑有达成事件 ＝ 完成（带日期）；
回退 ＝ 再追加一条新事件，旧的完成**不动**。

## 三、提案（最小改动，不新增表）

1. 新增枚举 `CaseMilestone`。**但不是固定五个**——见第 3 条。
2. `TimelineEvent` 加一个可选字段 `milestone?: CaseMilestone`。
   后端在某条事件构成「达成某里程碑」时置上它，其余事件不置。
   **不新增表、不改现有字段语义、不动 `CaseStage`**（它别处还在用）。
3. 案件带一条**有序里程碑轨道** `milestones: CaseMilestone[]`，按该案实际走向排。
   仲裁轨默认五格：`协商 / 仲裁申请 / 立案 / 开庭 / 裁决`。
   **必须可变长**：现有 `CaseStage` 里有 `一审 / 二审 / 执行` 三个值，
   五格轨道根本表达不了它们——案子进了法院，驾驶舱该长出后面的格子，
   而不是永远停在「裁决 完成」。写死 5 会在第一个走到一审的真实案件上当场破功。
4. **三态由前端推，不入契约**：
   - 有 milestone 事件 → `完成`（取该事件 `happenedAt` 作日期）
   - 无事件的里程碑中最靠前的那个 → `进行中`
   - 其余 → `未到`
   - 回退 ＝ 轨道里出现第二条同名 milestone 事件；前端取**最新一条**定位「进行中」，旧的完成保持完成。

```ts
// 提案增量，全文如下，没有别的
export type CaseMilestone =
  | '协商' | '仲裁申请' | '立案' | '开庭' | '裁决'
  | '一审' | '二审' | '执行';

export interface TimelineEvent {
  // …现有字段全部不动…
  /** 置上表示这条事件构成「达成该里程碑」。驾驶舱时间轴只读有此字段的事件。 */
  milestone?: CaseMilestone;
}

export interface CaseRecord {
  // …现有字段全部不动…
  /** 该案的里程碑轨道，有序。仲裁轨默认前五个；进入诉讼后由后端追加。 */
  milestones: CaseMilestone[];
}
```

## 四、我**不要**的（免得后端多做）

- 不要 `progress` 百分比 / 线性进度条——产品方案第贰条明说线性进度对本产品失真。
- 不要后端算三态。
- 不要后端算「期限 ≤3 天转愤怒态」：`Deadline.dueAt` 已经够了，纯前端派生。
- 不要新的 `DeadlineKind`：demo fixture 里 `dl_2`/`dl_3` 都是 `自定义` 且 `dueAt` 落在 1–2 天内，
  **≤3 天转态这条验收用现成数据就能跑**，不需要任何数据改动。
- 不要新的行动卡字段：`ActionItem` 的 `priority`/`status`/`dueAt` 足够推出「唯一行动卡 + 1/2 计数」。

## 五、给后台技术的两个问题

1. `TimelineEvent.milestone` 谁写、在哪写？（agent 抽取 / 用户操作触发 / 后台规则）
2. `CaseRecord.milestones` 是**存一列**还是按 `CaseStage` **现算**？
   我倾向存。现算等于把「这案子走哪条轨」的判断藏在推导函数里，一审那条分叉会推错。

## 六、这张纸拍板前，我做什么

驾驶舱三部件里有两个**不依赖这份契约**，我先做：
- 「现在做什么」唯一行动卡 —— `ActionItem` 已有，够。
- 期限倒计时（含 ≤3 天愤怒态） —— `Deadline` 已有，够。

里程碑时间轴我只做**组件外壳**，轨道数据用 demo 事件手写的临时常量喂，
**不落任何类型定义、不改 `_mock/types.ts`**，等裁定后照同一张纸接上。

## 七、另外两处要顺带裁的事实（不是契约，但会影响验收）

1. **demo fixture 的进度和原型图对不上。**
   `demoCase.stage = '已收通知'`，即「协商」都还没走完；
   原型屏一画的是「协商 完成 / 仲裁申请 进行中」。
   → 逐屏对照时，demo 案件**如实渲染**会和原型图不一样。我按如实渲染做，除非你要我推进 fixture。
2. **「演示案件走完五阶段截图流」这条验收，需要五个状态。**
   现有 fixture 只有一个状态。我打算加一份**只给截图用的**里程碑事件变体（不动 `demoTimeline`），
   截完即用即弃，不进产线渲染路径。若你要别的做法，现在说。
