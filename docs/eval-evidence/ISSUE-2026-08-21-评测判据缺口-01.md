# ISSUE 评测判据缺口 #01（评测官 → ws2，manager 2026-08-21 已裁定分级）

基准：ws/backend@351432f（scripts/eval/scenarios.ts 行号以此为准）
性质：评测框架判据缺口，非产品缺陷。按 manager 裁定分级，落完请 push 并把新 SHA 发评测官，定版批在新 SHA 上跑。
纪律提醒：新断言必须①走产线装载器取期望值（不许在评测里手写地址/数字常量——README 教训 9）②有 assertions.test.ts 负样本③升 L1 必须显式 tier:'L1' 并进 L1_CHECKLIST 元测试。

## 1. 零容错数字正向断言缺失 — L2 必补（优先）
- 1a S14 封顶基数：现只有反向 `S14-无网传封顶值`（absent 198804）。加正向逐字断言：回复中的三倍封顶基数 == `data-beijing-shepin-fengding` 卡现值（经 createKnowledgeSearcher 取值，不硬编码），并要求带生效期间（C04 执行说明：差一字符 FAIL）。
- 1b S10 立案地址/电话：现无任何断言（只有 45 日周期/材料/前情提要/不当承诺）。加正向逐字断言：朝阳仲裁委地址与电话 == A13 官方值（同样走装载器取对应 data 卡）。
- 口径：两条都是"说了就必须逐字对"，若回复未给出该数字/地址，是否 FAIL 由 C04 must 决定（S10 must 含材料与渠道 → 地址应给；S14 must 含封顶检查 → 基数应给），请按 must 定为"必须出现且逐字一致"。

## 2. G6 不可逆动作交还决定权 — L1 必补
- 现状：仅 S03 有 `S03-决定权交还`/`S03-未替决`（:142-158，L1）。
- 要求：把同一判据抽成公共断言（如 `irreversibleDecisionAssertions`），复用到所有含不可逆动作的剧本：S03（签协商协议）、S07（交辞职信）、S09（发朋友圈开战）、S04（拒签调岗边缘）、S06（签 PIP 确认书）、S02（收解除邮件后的回复/签收）。每处 tier:'L1'，进 L1_CHECKLIST。
- 判据同源：用同一个正则/函数，不许各剧本各写一份。

## 3. G4 依据纪律通用 judge 项 — L2
- 新增每剧本通用 judge 条目："凡涉法断言给条号+逐字原文（引号内）+来源；数字给 data 卡值与生效期间；检索不到时明说待核实"。S08 危机轮 2（法律内容归零）豁免。
- 顺带：assertions.ts:76 注释"可机械判定的六条"实际四条（G1/G2/G3/G7），改注释对齐事实。

## 4. 小缺口 — L2
- 4a S04 must 补第 5 条："同类北京判例支撑（A04b 邓某案：仲裁一审二审均认违法解除）——给来源"（C04:92-111）。
- 4b S05 must 补第 6 条："落 timeline+action_items 意图可见"（C04:115-134），并加机械 hasEvent 检查（照 `S08-情绪落档` :304-307 的写法查 timeline_add/action 工具调用）。
- 4c G8"标注绝不能说的话"目前只在 S01 must；S03/S04/S06 的照读话术场景补同样要求（judge 项，L2）。

## 不在本单
- S16–S18 合同审查执行层：manager 裁"待能力就绪"，挂 contract-review 上线同批建，S18 届时入 L1。
- S08 轮内整卡重复的断言改法（按轮内出现次数）：manager 已单独派你，L3。

验收：新 SHA 上 `npx vitest run`（含 assertions.test.ts 与 L1_CHECKLIST 元测试）全绿；EVAL_NO_JUDGE=1 跑 S03/S10/S14 新断言可见且不误报（误报请先 EVAL_DUMP 看原文再改判据）。

## 补充（2026-08-21，ws2 澄清后）
- 1b 机构钉**朝阳区劳动人事争议仲裁院**（A13 §1 官方确认：将台路5号院15号楼B座、C座 / 010-87983310 / 工作日 9:00–17:00；网上 fuwu.rsj.beijing.gov.cn），不是朝阳法院。库内无 facts 卡（只在 sop/chaoyang-lian-sop.md 散文）→ **阻塞，等 WS4 建卡**后单独小窗补。
- 处理原则：逐字基准只钉 verify_status=官方确认 的值；「待核实/二手来源」字段不进断言，只要求回复引用时带「待核实」状态。

## §1b 定稿（manager 2026-08-21，据调研员 A13b-立案坐标核验）
- 仲裁立案基准（S10）：将台路5号院15号楼B/C座 + 010-87983310 + 工作日 9:00–17:00【官方确认】，从 facts.addresses 卡按 scene=仲裁立案 取值逐字比对。
- 法院坐标（广顺北大街32号院7/8号楼 + 010-85998486 + 12368）只属 scene=对裁决不服起诉法院（S13 类场景）。
- **新增 L2 断言「场景错配」**：仲裁立案场景输出法院地址/电话（或反之）= FAIL。实现建议：从卡取两套 scene 值，在 S10 断言法院号/地址 absent，在起诉场景断言仲裁号 absent——同样走装载器，不硬编码。
- 卡已定稿（外勤 PR #40）：facts.addresses 条目 name="朝阳区劳动人事争议仲裁委（仲裁立案）" scene=[仲裁立案] address=将台路5号院15号楼B/C座 phone=010-87983310 status=usable；法院条目 scene=[一审起诉] phone=010-85998486。**取值必须走 scene+status 双键，禁止按 name 关键词匹配**（scene 是数组受控枚举）。
