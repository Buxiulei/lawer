# 存证：M1 发版所依据的跑批未加载错版本知识库（评测官，2026-08-25）

**背景**：2026-08-25 查明 `app/src/lib/knowledge/index.ts:87` 以 `process.cwd()` 解析知识库目录，cwd 不同会解析到**另一个 worktree 的知识库**（`~/caiyuan-ws/knowledge`，该目录确有 index.json）。若跑批曾命中此路径，**发版结论所依据的转录就是用错版本法条产出的**，须全部作废。本件是对"未发生"的核实存证，非口头确认。

## 一、核实对象（发版结论所引的四批，共 53 份证据文件）
| 批 | 行为 SHA | 证据文件数 | 去重 retrievedIds |
|---|---|---|---|
| 候选批 | b7d0589 | 28 | 94 |
| S14 迷你批 | acb2133 | 3 | 24 |
| 决胜轮 | 175cad3 | 7 | 30 |
| 全量 15 终验 | 175cad3 | 15 | 92 |
| **合计（去重）** | — | **53** | **118** |

## 二、三条独立证据（任一成立即排除"加载了 WS4 库"，三条同时成立）
**证据 1 · 结构性（ws2 提供，评测官复算）**：`knowledge/index.json` **受 git 版本控制**，worktree checkout 到 SHA X 时知识库必然是 X 那一版。逐 SHA blob（我用 `git rev-parse <SHA>:knowledge/index.json` 独立复算，与 ws2 报数完全一致）：
- `175cad3` / `655c973` → **bd6df5d4**｜`b0871a6` → **694184f9**｜`main b82af28` → **0a154406**
**证据 2 · 执行路径**：`run-batch.sh` 第 20 行 `cd app` 后才执行 `npx tsx ../scripts/eval-agent.ts`，故 `process.cwd()=<clone>/app` → 解析为 `<clone>/app/../knowledge` = **本 clone 自己的 knowledge**；本 clone 与 WS4 worktree 是**两个独立目录**。
**证据 3 · 穷举比对（非抽样）**：把上述 53 份证据文件里的 **全部 118 个 retrievedIds** 与两个库逐一比对——
- 在本 clone 解析不到的：**0**
- **只存在于 WS4 库、本 clone 没有的：0**（此项若非零即为加载错库的铁证）
**差分有效性自证**：两库内容指纹**不同**（本 clone `d556c0bc` vs WS4 `24739d26`），故证据 3 的差分**具备区分力**——不是"两库一样所以怎么比都零差异"。

## 三、结论
**M1 发版结论所依据的跑批与转录，未加载过其它分支的知识库；发版结论不受 cwd 缺陷影响，无需作废任何批次。**

## 四、随本件落地的执行物（防复发，已改入 run-batch.sh）
1. **三维落款补齐第三维**：META 增记 `kb_index_blob`（`git rev-parse HEAD:knowledge/index.json`）——A17 已有「判据语义版本 × 被判行为 SHA」，补上**知识库版本**，三者齐才算可复现。
2. **开批前置校验**：打印并校验实际解析到的知识库**绝对路径**与**卡数**，路径不等于 `<clone>/knowledge` 或卡数 <100 **拒绝开批**（写 FAILED 退出）。
3. **操作纪律（ws2 提醒）**：本 clone 现已在 main(b82af28，知识库 0a154406)。**要评哪个 SHA 就 checkout 哪个 SHA**，否则会出现「代码旧、知识库新」这种谁也没打算做的组合——**而它不报错**。
