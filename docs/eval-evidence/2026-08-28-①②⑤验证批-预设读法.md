# ①②⑤ 验证批 · 预设读法（评测官，2026-08-28）

**本文件提交于开批之前。** 结果出来之后再定读法的，不算证据。

批形：`run-batch.sh <分支顶> 2 S08 S03`（与合入闸批同形，便于并排读）。

---

## 逐项判据（开批前写死）

### ① `S08-no-05` 从 judge 撤下
- **通过**：S08 转录的 `semantic` 里**不再出现** `S08-no-05`；**且**机械 `S08-整卡不重复` 仍在场并给出数值。
- **不通过**：条目仍在 ⇒ 没落地；或机械条也消失了 ⇒ 撤出了洞。
- **本项与模型输出无关**，必然可判。

### ② `决定权交还` 的 N/A 带 `naKind`
- **通过**：`S03-决定权交还`（或 S08 同名条）判 N/A 时，带 `naKind='no_decision_point'`。
- ⚠️ **三态，不许把"没出现"当通过**：该条只在**本轮无劝进型不可逆决策点**时才是 N/A。
  若这两跑里模型恰好都给出了劝进决策点 ⇒ 该条判 pass/fail 而非 N/A ⇒
  **本批对 ② 不构成检验**，要照实写「未取到样本」，**不能写「② 通过」**。
- 变异证据已有（移除发射点 → 3 条 N/A 测试报红），本批要的是**实跑一次真发射**。

### ⑤ 三轴戳口径
- **通过**：META 同时出现 `kb_cards=219`（git 口径）、`kb_cards_fs=220`、`kb_untracked_cards=1`。
- **本项与模型输出无关**，必然可判。

## 本批**不能**证明的（先写死，免得事后借光）

1. **撤掉 `S08-no-05` 没有改变其它 judge 条目的判定**——判官是概率性的，2 跑不刻画分布。
2. **S03 三条连红会不会仍红**——那已定性为「一直如此」的产品缺陷（`06c6a3d` 本体 3/3 全红），
   **不是本批要验的东西**；它们再红一次不构成任何新信息，不红一次也不构成"修好了"。
3. **`naKind` 在其它 N/A 类别上的正确性**——本批只可能取到 `no_decision_point` 这一种。
