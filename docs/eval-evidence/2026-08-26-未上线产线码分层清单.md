# 未上线产线码 · 三类分层（manager 2026-08-26 点名要，用于定滚更内容）

**基线**：线上 `00ce720` ≡ `origin/main`（`crisis.ts` blob 相同，`d74a1dc`）。
**输入**：`git diff --stat origin/main ws/backend -- app/src` 的全集，**不重新扫**。
**已从本清单剥离**：滚更包 `ws/hotfix-leverage = fbafe4e`（`d10a00c` + `c009c1c` + `972e31e`），manager 已签。

> **A 类的判据不是"我写的时候测过"**，必须是可指名的测试文件或跑批/回放证据。
> 下面每一条都给了文件名或实测数字；给不出的，一律归 B。

---

## A 类 · 完成且已验证

### A-1 公道值账本接线（`3a3980f` 的 billing 半边 + `backfill.ts` / `reconcile.ts` / `features.ts` / `migrate.ts`）
- **单测**：`app/src/lib/agent/__tests__/billing-wiring.test.ts`（152 行）、`lib/billing/__tests__/backfill.test.ts`、`lib/db/__tests__/reconcile.test.ts`
- **跑批级证据（2026-08-26 补）**：`scripts/acceptance-billing.ts`（`a8ab497`）——
  **真实模型跑一轮 → 磁盘库 → CLI 三向对账 rc=0**：
  `token_usage: ref=turn-2 feature=intake model=DeepSeek-V4-Pro-0813 api_model=deepseek-v4-pro 四桶=6424/4273/25216/0 cost_li=54226`
  `gongdao_ledger: ref=turn-2 type=消耗 delta=-55 feature=intake`
  外加 **4 条变异 + 1 条反向对照**全过（每条钉住"红的是哪一条"，不是"红了就算"）。
- **为什么单测不够**：验收标准是「**在非空表上跑通**」。`:memory:` + 假 provider 证明得了接线在，
  证明不了「真模型跑完一轮、落到磁盘库、CLI 读得到」。**空表报绿是掩盖层，正是这件事最初被定 P0 的原因。**

### A-2 注入产物可观测接口（`eb898da`）
- **单测**：`scripts/eval/assertions.test.ts` 三态各有样本，且专测「三者签名两两不同」（防"写了三态但两态同路"）
- **回放**：**24 条真实转录，逐轮正文 sha256 与 added 集合完全一致**（新旧两树各跑一次对比，非推断）
- ⚠️ **这里必须区分一件容易搞混的事**：
  > **同一个"零差异"，证明力取决于声明的方向。**
  > 这条改动**声称的就是"不改变行为"**——对一个"无变化"的声明，零差异是它的**正面证据**。
  > 而杠杆闸第二层声称的是"改变某类误剥"，零差异只说明**样本够不着它**，不是验证通过。
  > 拿本条的绿去类推那条的绿，就是把两种相反的声明当成同一件事。

### A-3 判据同源·机制版（`58d4e26`，今天）
- **单测**：74 文件 1622 绿；五条负样本（假 FAIL 已修 / **假 PASS 已修** / 旧转录判定不完整 / 首段口子由独立断言接住 / 正常首段不误伤）
- **两条能力级守卫**：底层检测器不许再导出（长回来就红）、首段切分只许有一份来源
- 它改的是**判据**不是产品行为，故不进滚更风险评估，但会改变此后成绩单的可比性——断代见
  `scripts/eval/results/README-判据断代.md`

---

## B 类 · 完成但未验证（有单测，**没有属于它自己的跑批/回放证据**）

> 这一类的共同形状与 `c009c1c` 当初完全一样：**上线时的默认假设是"有单测就够了"。
> 而单测证明的是"我想到的那些情形对了"，不是"真实语料里它没把事情弄坏"。**

| 编号 | 提交 | 改了什么 | 行为面？ | L1 相关？ | 有的证据 | 缺的证据 |
|---|---|---|---|---|---|---|
| **B-1** | `320fc41` | 保底渲染触发条件改为「核心位 ∧ 该条逐字原文不在正文中」 | **是** | **是（G4「给少了」）** | `citation-block.test.ts` | **无跑批**。它直接决定**用户手里有没有那段可照念的原文** |
| **B-2** | `28c8e0d` | 轮级归属改严格正向，堵回"邻条原文替本条免责" | **是** | **是（G1/G4 归属判定）** | `citation-block.test.ts` | 无跑批 |
| **B-3** | `9b93e82` | 空手感知：判据从「有没有卡」改为「卡够不够格」+ **空包告知指令入 prompt** | **是**（改喂给模型的指令，`prompt.ts +53`） | 间接 | `empty-pack.test.ts` | 无跑批 |
| **B-4** | `34a93ef` | **危机轮豁免空包告知**：检测照记、指令不注入 | **是** | **是（危机轮）** | `empty-pack.test.ts` | 无跑批（作者给的是**设计论证**：「收益为零、风险为正」） |
| **B-5** | `c2c0dd5` | `KNOWLEDGE_MISS` 归位：挂「是否注入」而非「是否检出」 | 是（notice 触发面） | 否（L2/L3） | `orchestrator.test.ts` | 无跑批 |

**关于 B-4 要说句公道话**：它的方向是**危机轮少注入一段**，即**保守向**；作者也写清了
「收益为零、风险为正——这是设计题不是实证题」。这个论证成立。
**但设计论证不等于闸**：它管的是"该不该做"，不管"做出来的是不是它"。
`c009c1c` 当初也有一个成立的论证，塌掉的是它的**前提**，不是论证本身。

**另有两个不构成风险项**：`98b7d97`（纯注释精确化）、`9650d4d`（补测试，无独立产线语义）、
`1deaeb5`（合 main，带进 `referral_offers` 建表——该表已在 main、只是没上线）。

---

## C 类 · 被闸锁着

| 提交 | 闸 | 今天的状态 |
|---|---|---|
| `a6a23bb` 杠杆闸第二层 | `docs/OPS.md`「已在主干但未放行上线的改动」条件 ② S08 两跑 | **跑了，无判别力（B==C 两跑皆是），闸不清，继续押后** |

---

## 单独回答：**除第二层那道闸，这 27 个文件里还有哪些"行为面且 L1 相关、本该有闸而现在没有"？**

manager 点名的三处，逐一给答案：

### `citation-block.ts +176` —— **有两条，是本清单里最该补闸的地方**
`320fc41`（B-1）与 `28c8e0d`（B-2）**都是行为面且 L1 相关，都没有闸**。
`320fc41` 尤其：它决定**用户拿去仲裁的材料里，那段法条原文在不在**。
**建议的闸（形状已经现成）**：在一批真语料上对照修前修后的「核心条**原文在场**计数」——
这与 S08 两跑用的是同一套离线重打分手法，`320fc41` 的效果**恰好可以在归档正文上看见**
（它是"往正文里加内容"的方向，**不是剥除**，所以不存在杠杆闸那种"回放只能看幸存者"的天花板）。
**这条闸比第二层那条好造**，因为被测的效果留在归档里。

### `orchestrator.ts +225` —— **有一条**
拆开是 7 个提交。其中 **`34a93ef`（B-4，危机轮豁免空包告知）是唯一 L1 相关的一条，没有闸**。
其余（账本接线、可观测留痕、`KNOWLEDGE_MISS` 归位、去重开关、杠杆闸接线）要么已在 A 类、
要么已在滚更包、要么非 L1。

### `knowledge/index.ts +42` —— **没有，且不需要闸**
`git diff --numstat 00ce720 ws/backend` = **`42 0`，零删除零改写，纯增量**：
导出 `isSubstantiveHit` / `countSubstantiveHits` + 把指标语义常量圈起来加注释锁定。
**它不改变检索行为**，且被 A-2 那次 24 转录字节级回放覆盖。
三处里唯一可以放心的一处。

> ⚠️ 但它埋了一颗**将来**的雷，写在这里免得被忘掉：那几个常量（`MIN_KEYWORD_LEN`、
> `matches()` 子串规则、`SUBSTANTIVE_MIN_SCORE`）共同定义了 `substantiveHitCount`，
> 而该指标同时是「空手感知」的触发判据与「空包率」运营指标的分母。
> **改这几个数不是调优，是改指标定义——昨天的 30% 与今天的 30% 不再是同一件事。**
> 代码注释已写明须 manager 同级审批。**今天不需要闸，改它的那天需要。**
