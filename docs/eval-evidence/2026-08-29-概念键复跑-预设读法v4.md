# 概念键复跑 · 预设读法 v4（评测官，2026-08-29）

**提交于外勤改键之前。** manager 裁：本案最后一轮，**C 过不过都出结论**
（过则整体上线；不过则词面键版本先上线、概念键另立慢单）。

前序：v1 `d3ef217` ／ v2 `460d20a` ／ v3 `cb4ce46` ／ 补词复跑成绩单 `d4fcd8b`。

---

## 〇、提交前固定一问（manager 升格为固定动作）
> **画通过线之前先问：这一轮的改动，在范围上能碰到几条用例？线只能画在它够得着的集合上。**

我在"线的范围"上连错三次（v1 跨两层写成单层线／v2 锚在被撤版本／v3 跨概念范围写成单一线）。
**本轮第四次在开批前被自己抓住**——见 §三 c-4。

## 一、为什么必须有新封存集 C（manager 裁）
> **B 已揭封即已烧毁。** 修法方向（键放宽到概念）来自 B 的反事实，**合法**；
> **但泛化验证不能再用 B**——否则这轮就是"对着揭封的留出集补词"。

C 共 6 条，**只存在于 scratchpad，不入仓**；本文件登记三份哈希，判后公开复算：
```
sha256(holdout-C.json) = 69896e7b1605349ae049b58ea1201d961b290c11440526665852fef466f279e8   ← 留出句全文
sha256(judgeC.test.ts) = 1ed623f4b871a4925b4d88c091650185f5229b27cdc17cd3fdf48a84f8703308   ← 判读代码
sha256(baseline-C.txt) = 2cda8289c00bde2dd6e725d951bb3a322b2311472b96bfd114f31b6c87fb71b0   ← **改键前基线**
```

## 二、C 的构成（3 + 1 + 2）
```
执行概念·新说法 3 条   期望翻中（均不含本轮任何现有别名键）
⚠️ 过宽探针 1 条        含现有别名键、但用户的需求不是执行
范围外对照 2 条         调岗／签字概念，本轮不补词 ⇒ 不该因此改变
```
**范围外那 2 条是「概念范围切分」这条判据本身的检验**：
若它们也跟着动了，说明改动溢出了声称的范围。

## 三、⚠️ C 的判据全部锚在 **C 的改键前基线**（不是绝对值）
改键前实测（已封存）：
```
c-1 赢了官司钱要不到          应召回 0/1  广度 0
c-2 劳动局判我赢了公司装死     应召回 0/1  广度 0
c-3 胜诉了但对方不给           应召回 0/1  广度 0
c-4 裁决书什么时候能下来       **已命中 sop-zhixing-sop**  广度 13   ← 过宽探针基线就是 1
c-5 公司让我搬到别的城市上班    应召回 0/1（范围外）
c-6 他们让我写个东西说自愿走的  应召回 0/1（范围外）
```
> **我原本把过宽探针的线写成"须 0"，而基线已经是 1——同族第四次。**
> **这次在开批之前就抓住了，因为我先取了基线再画线。**
> **实质发现**：词面键版本**已经**把强制执行卡带进「问送达时限」的 query
> （ 与  双键同时命中）。
> **这一格在原 48 条评测集上看不见**——manager 那句"词面键版本零回归"，
> **在我原口径下成立，在 C 这一格上不成立。落地时请带上这个限定。**

### 判据
```
① 执行概念内 c-1/2/3 翻中 **≥2/3**
② 过宽探针 c-4 **不得比基线更差**（基线命中 1 ⇒ 改键后仍 ≤1；变 0 记为改善）
③ 范围外 c-5/c-6 **应召回不得上升**（上升 ⇒ 改动溢出声称范围，须解释）
④ 原 48 条：硬闸（绊线全 0、真实广度中位 ≤8）与不退化（锚真基线 b07fee0）全部照旧
```

## 四、两字键的额外风险（manager 点名）
`裁决` 是 **2 字键**，侵入面比四字大。除 C 之外另加两条判读：
```
真实广度中位          必须仍 ≤8（词面键版本是 3；若跳到 8 以上即否决）
六句噪音自查          外勤按标准流程做；我复核其结果，不代做
```

## 五、本轮不能证明的
1. C 只有 6 条：**能证伪"只在烧毁集上有效"，不能证明泛化性好**。
2. 不能证明产品变好——这把尺只量检索有没有把料递到 agent 手上。
3. `判了` 若一并加入，其出处需单独论证（manager 已要求另证），**不在 C 的判据内**。
