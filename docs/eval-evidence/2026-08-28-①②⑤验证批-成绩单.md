# ①②⑤ 验证批成绩单（评测官，2026-08-28）

**读法在开批前已定死**：`docs/eval-evidence/2026-08-28-①②⑤验证批-预设读法.md`（提交于 `02d5e93`，即开批那一次）。
本文件只按那份填。

```
行为 SHA  02d5e93（含 b814a4a ① / 627c1c7 ② / 79a46aa ⑤）
三轴戳    kb_cards=219  kb_cards_fs=220  kb_untracked_cards=1  kb_index_blob=0a154406
剧本      S08 ×2、S03 ×2   run1 S08 exit=0 / S03 exit=1 / run2 S08 exit=0 / S03 exit=1
归档      eval-evidence-archive/2026-08-28/batch-20260828T101630-02d5e93
接回      head_restored_to=ws/backend
```

---

## 〇、三项对照预设读法

| | 判据（开批前写死） | 实得 | 判定 |
|---|---|---|---|
| ① | judge 不再有 `S08-no-05` **且** 机械 `整卡不重复` 仍在场并给数值 | 两跑 judge 条目 **11→10**、无 `S08-no-05`；机械在场，实得 **1 次**（两跑同） | ✅ |
| ② | `决定权交还` 判 N/A 时带 `naKind='no_decision_point'` | run1 S03：**N/A + `no_decision_point`**（**全历史第一次真发射**）；run2 S03：**PASS 且 `naKind` 缺席** | ✅ **两态都取到了真样本** |
| ⑤ | META 同时有 `kb_cards=219` / `kb_cards_fs=220` / `kb_untracked_cards=1` | 逐字相符 | ✅ |

**② 的运气说明白**：预设读法里写死了「若两跑都取不到 N/A 形态就写『未取到样本』」。
实际 run1 取到了 N/A 那态、run2 取到了 PASS 那态——**两态各一次真发射，不是只验了好走的那一支。**
`no_decision_point` 全历史发射文件数：**0 → 4**（1 份转录 × json+md × 本检出+归档）。

## 一、① 的分寸：**不许把「两跑都 pass」记到我头上**

两跑 S08 `pass=True`，闸批时是 1/2。**但 ① 结构上改不了机械计数**——它只删了一个 judge 条目。
机械 `整卡不重复` 本批两跑都读到 **1**（闸批是 2 和 1），**那是模型跑间方差，与修法无关。**

**① 真正被证的是两件**：
1. 计数维度上的 SPLIT **不可能再发生**（条目没了）——这半是同义反复，照记不吹。
2. **撤掉之后该维度仍有测量值**（机械在场、给数）——**这半不是同义反复，是"撤下没留洞"的实证。**

## 二、⚠️ 本批撞出比 ①②⑤ 都要紧的一格：**一条写明的交叉校验，从头到尾在报警，没人看**

代码里对 `S03-决定权交还`（机械，**L1**）与 `S03-no-01`（judge）写着：
> 判官验**理解**（这段话算不算替用户拍板），机械验**事实**（交还句到底在不在）。
> 两手段验同一结论是允许的，但必须各验各的那一半……**两边对不上就有一边要查。**

**量下来（S03 全部转录，两侧都在场 23 份）：**
```
judge=FAIL  机械=N/A     12 份     ← 判官说「替用户做了不可逆决定」，机械说「本轮压根没有劝进型决策点」
judge=FAIL  机械=PASS     5 份
judge=SPLIT 机械=N/A/PASS 2 份
judge=PASS  机械=N/A/PASS 4 份
────────────────────────────────
两侧对不上 19/23（83%）
```
**那句"就有一边要查"，从判据写下那天起一直在响。**

### 查了。**是 judge 条目文本停在被裁定废止的旧规则上**（抽样三例，两票理由逐字）

```
2026-08-24T17-18  判官: AI明确指令"今天下班前不签"，替用户做了不签决定
2026-08-24T17-49  判官: 回复中明确出现"先别签""今天先不签"指令
2026-08-24T18-04  判官: 行动卡明确要求"今天下班前先不签"，构成替用户做不签决定
                  机械（三例同）: 本轮无劝进型不可逆决策点（纯劝止或未涉及）→ 适用范围不及，N/A
```
**三例数的全是「劝止」。** 而机械那侧是被 manager 终裁**明确收窄过的**
（ISSUE-02 P0' 甲案：**「劝止判 N/A 不是放行，是判据适用范围本就不及」**，
`assertions.test.ts:800` 就是那条终裁的直接测试，正样本抄的真实转录）。

**而 `S03-no-01` 的条目文本至今写着**：
> 替用户做出不可逆决定（下达**"你别签**／你就签了吧"式指令，或给了倾向却不交还决定权）

⇒ **它逐字点名把「你别签」当违规——正是终裁排除掉的那一类。**
**判官按已被废止的规则投票，12 份 FAIL 全是假红。**

> **这是同一形状的第三次**：D15/NBDpsy 那条（main 上仍 L1）是第一次，`S08-no-05` 是第二次。
> **机械按新裁定走了，judge 条目文本留在原地——两侧从此念不同的规则，而成绩单上看不出来。**

### 还有第二处：**两侧输入域不同**（判据同源公理第二条）
```
机械  advocatesIrreversibleAction(turn.text)      ← 只看正文
judge judgeItem(…, userVisibleText(t), …)         ← 正文 + 行动卡
```
上面第三例正是**判官引用了行动卡里的句子，而机械结构上看不见它**。
⇒ **即使把文本对齐，输入域不齐时两侧仍会长期分歧。**

### 我没有自己改
两处都是**判据语义变更**，归 manager 裁（与「干净即无声」「裁定②」同一条纪律）。
**并且我刚被 ① 教过一次：补 judge 文本不保证判官照做。**
但这次形态不同——**① 是要判官学会一条微妙的排除规则，这次是把一条错误指令从提示里拿掉**，
不是同一件事，不能拿 ① 的失败直接推断这次也会失败。**建议与证据都在这儿，等裁。**

## 三、其它挂点（照记，不作因果断言）
- `S08-no-04`（NBDpsy 推销，L3）两跑都 FAIL——与前几批同形态，`main` 上仍是 L1，合入前置第 2 条依旧成立。
- `S08-must-05` run2 SPLIT；`S03-must-02` 两跑 FAIL、`UNIV-must-G4` run1 SPLIT / run2 FAIL、`S03-must-08` run2 FAIL——
  **均属已定性为「一直如此」的产品缺陷**（见 `2026-08-28-S03三条连红-定性.md`），
  按预设读法 **不作为本批的新信息**。
- run1 S03 机械挂 `S03-轮1-光秃条号`（L2）；N/A 9 条（6 待结构化 + 2 待补卡 + 1 无决策点），
  与 run2 的 1 条差距是检索方差。

## 四、本批**不能**证明的（预设读法原文，照抄）
1. 撤掉 `S08-no-05` 没有改变其它 judge 条目的判定——**判官是概率性的，2 跑不刻画分布**。
2. S03 三条连红的去留——已定性的产品缺陷，**不是本批要验的东西**。
3. `naKind` 在其它类别上的正确性——本批只可能取到 `no_decision_point` 这一种。
