# 检索对比批 v2 成绩单（评测官，2026-08-29；③ 别名层 `dbc4400`）

读法写死于 `460d20a`（**在 `dbc4400` 之前**）。
**核准：v2 读法与 v1 成绩单在 `dbc4400` 上均差异 0 行——一字未被动。**

```
真·基线 b07fee0 ／ ②词缀（已撤）01b3349 ／ ③别名 dbc4400
前置逐条核过：卡数仍 218、kb_index_blob 同为 0a154406、评测集差异 0 行、
              knowledge/ 新增的是 aliases.json（数据文件，非卡）、DOMAIN_MIN_DF 已归零（②确已撤）
```

---

## 〇、按 v2 逐条裁决

| 判据 | 线 | 实得 | 裁决 |
|---|---|---|---|
| **硬闸①** 四条绊线实质命中 | 全 0 | **全 0** | ✅ |
| **硬闸②** 真实广度 `limit=218` 中位 | ≤ 8 | **1** | ✅ **回到基线水平** |
| **必修项** `rw-misc-2` 应召回 > 0 | 必须 | **2/2**（且只捞 3 张） | ✅ |
| 主判据·形变层翻中 | ≥ 4/5 | **2/5** | ❌ |
| 不退化·孪生臂应召回 | 9/9 | **9/9** | ✅ |
| 不退化·真实台词应召回 | ≥15/42 | **11/42** | ❌ **见 §二：线锚错了** |
| 不退化·用户侧应召回 | ≥28/77 | **21/77** | ❌ **同上** |

**三数（报数强制）**：`应召回 21/77`｜`真实广度中位 1`｜`捞满捞错 7`（基线 7，持平）
广度均值 10.4 → 21.7，**最大值 103 → 103（最坏情况未变差）**。

## ⚠️ 二、三条"不退化"线全部锚错——**这是我 v2 的缺陷**
v2 把三条不退化线锚在 **"返工前水平"**（15/42、28/77），
而那是 **② 词缀版的数字，② 已被 manager 判退回并撤销**。
> **拿一个被否决版本的成绩当"不得倒退"的基准，是错的。**
> 不退化应当锚在**真·基线**（`b07fee0`）。

对真·基线重判：
```
真实台词应召回   9/42  → **11/42**   ↑
用户侧应召回    13/77  → **21/77**   ↑
孪生臂          9/9   → 9/9         =
真实广度中位      1    → **1**        =   （②版是 25）
```
**⇒ 对真·基线，三项全部改善或持平，零退化。**

**这与 v1 的缺陷同类**：v1 是把跨两层的目标写成单层线，v2 是把线锚在一个后来被撤销的版本上。
**两次都是读法作者对"被测对象将会是什么"预判错误。** 照记，不自己改判。

## 三、形变层 2/5 的归因
```
✅ rw-misc-1   工资拖两月能直接不干吗        实质8 应召回1/2
✅ rw-misc-2   怀孕了公司把我调到外地        实质3 应召回2/2   ← 必修项，且很准
🔴 S12-t1      裁决下来20天公司一分钱没给     → 需别名 裁决书→申请执行
🔴 rw-misc-4   裁决书下来了公司不给钱怎么办    → 同上
🔴 rw-qianzi-1 公司让我签协议我该签吗         → 需别名 签协议→协商解除协议
```
**三条未翻中的都缺具体别名词，不是机制缺陷。** 词表内容归外勤（manager 裁）。
后台技术明确**没有自己加词再报绿**——**机制到位、词表未填，这两件事该分开记。**

## 四、加分项：同义层 2/4，**含旗舰句**
v2 写明同义层本轮不判、翻中算加分：
```
✅ rw-caiyuan-1   我被裁了怎么办              应召回1/3
✅ rw-peichang-2  **我干了5年月薪2万被裁该赔多少**  应召回1/2  ← v1 的单条否决项，现在过了
🔴 rw-peichang-1  被裁了能拿多少钱            实质8 应召回0/2
🔴 rw-cuiqian-1   HR催我今天必须签字          实质0
```

## 五、这一版为什么广度没炸（机制形状，值得记）
② 是**改匹配规则**（词缀互含）⇒ 广度增量随语料与 query 长度膨胀，中位冲到 25。
③ 是**扩 query**（命中别名就把规范词接到 query 尾部，之后仍走原来的整句子串匹配）
⇒ **匹配规则一个字没改，广度增量的上界就是别名表本身。**
> **这两版的区别不是参数，是形状。** 一个改的是判定规则，一个改的是输入。

## 六、本批仍不能证明的
1. 不能证明产品变好——这把尺只量检索有没有把料递到 agent 手上。
2. **不能证明别名表内容对**——本批只验机制；8 条种子词的正确性归外勤审。
3. 48 条不是用户真实提问分布的样本。
