# 概念键终判成绩单（评测官，2026-08-29；`a6453ae`）

读法 v4（`c965691`，**a6453ae 的祖先，差异 0 行**）。**封条三份判前判后复算全部一致，C 现已揭封入仓。**
前置：卡数 218、blob `0a154406`、评测集差异 0 行。

---

## 〇、裁决：**不通过**（C 的主判据 0/3）

| v4 判据 | 线 | 实得 | |
|---|---|---|---|
| ① 执行概念内翻中 | ≥2/3 | **0/3** | ❌ |
| ② 过宽探针不得比基线更差 | ≤1 | 1（持平） | ✅ |
| ③ 范围外不得上升 | 不升 | c-5/c-6 均 0，持平 | ✅ |
| ④ 硬闸·绊线 / 广度中位 | 全 0 / ≤8 | 全 0 / **3** | ✅ |
| ④ 不退化（锚真基线 `b07fee0`） | 孪生 9/9、台词 ≥9/42 | 9/9、**11/42** | ✅ |

## 🔑 一、决定性证据：**收益完全局限于已揭封的集合**
```
B/A 里的执行概念 2 条（**改键的依据来自它们**）    ✅ 都命中
C 里的执行概念 3 条（**新说法，未被任何人看过**）   🔴 全部未中
```
**原因——键放宽了，但仍然是词面：**
```
「仲裁委判了公司还是不给钱」   命中键 判了      ← B 里那句，逐字含「判了」
「劳动局判我赢了公司装死」     命中键 **无**    ← 「判我赢了」不含连续的「判了」
「赢了官司钱要不到」           命中键 **无**
「胜诉了但对方不给」           命中键 **无**
```
> **四字键 → 两字键，是把词面放宽，不是把键提到概念层。**
> **每加一个词面键，只救那个词面。**

## 二、这条路的性质（比通过与否值钱）
```
a6453ae 相对 f6603aa（词面键版）在我的尺上：
  应召回     23/77 → **22/77**（−1）
  捞满捞错     7   → **8**（+1）
  广度中位     3   →  3
  C 执行概念  0/3  →  0/3
```
⇒ **本轮改动在未见过的说法上买到零收益，并付了 −1 应召回、+1 误捞的代价。**
它确实救活了 5 条**已知说法**（A/B 各 1 + 外勤自测 3），**但那些是已揭封或自选的**。
> **结论不是"做得不好"，是这条路的形状：它是逐词枚举，不是概念泛化。**
> 泛化要么靠同义词层/向量召回，要么承认它就是一张要长期人工维护的词表。

## 三、外勤如实标价的那一格，我照读法判
他主动声明双挂的代价：**执行卡在文书类问句里陪跑第二**，占 `MAX_INJECTED_PACKS` 一格。
我的 `c-4`（过宽探针）正是这一格：`裁决书什么时候能下来` **仍命中强制执行卡**。
**v4 判据是"不得比基线更差"** ⇒ 基线 1、现在 1 ⇒ **判过**。
> **他没有为这一格辩护，只要求它"被知道而不是被忽略"。** 现在它被写进了成绩单。

## 四、两件他主动报的分支事实（我已核实）
1. **`rw-qianzi-1` 是待合并不是缺词**——#91 给卡加的 `让我签`/`该签吗` 在 `main` 有、本分支无。
2. **`a6453ae` 未跑 CI**——workflows 只在 `base=main` 触发；唯一验证是他本机全量（2022 passed）。**那是口供不是 CI。**
   本分支索引另有 31 个数字死词（main 已修），本尺未触及。

## 五、封存件揭封（哈希判前判后均一致）
`docs/eval-evidence/sealed-C-2026-08-29/`：留出句全文、判读代码、改键前基线。
**C 的构成 3+1+2**，其中 **范围外 2 条是「概念范围切分」判据自身的对照臂**——本轮它们持平，该判据成立。

## 六、给 manager 的落地建议（不是裁决）
1. **按你的终局条款：走 fallback。** 但两版在我的尺上近乎等价（22 vs 23），**若要选，`f6603aa` 略优**。
2. **无论上哪一版，成绩单必须并列摆出**：`应召回 13→22/23` 与 **`c-4 已知过宽（问送达时限捞到强制执行，成因双键同时命中）`**。**不许只报前者。**
3. **概念键慢单的目标要改写**：不是"再多加几个词面键"，而是**同义/近义召回**——本批已证逐词枚举不泛化。
