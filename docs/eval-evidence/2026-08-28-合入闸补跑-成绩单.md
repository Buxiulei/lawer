# 合入闸补跑成绩单：S08 + S03 ×2（评测官，2026-08-28）

**这是 `ws/backend → main` 的闸批。** 按 manager 裁定「补跑」的三项要求逐项交账。
读法沿用 `2026-08-27-改名行为面-预设读法.md`。

```
行为 SHA  5dbca1665b11122f7b1c4f09309d5ab33c17b400   （324f425 / 95c0f0e / 0220799 均为其祖先，开批前逐条验）
三轴戳    kb_index_blob=0a154406   kb_cards=220
剧本      S08 ×2、S03 ×2
          run1 S08 exit=1 / run1 S03 exit=1 / run2 S08 exit=0 / run2 S03 exit=1
归档      eval-evidence-archive/2026-08-28/batch-20260828T092152-5dbca16
接回      head_restored_to=ws/backend（本地与远端同为 5dbca16，树干净）
```

---

## 〇、结论（先说没闭合的）

> **我不写「验证批通过」。** 四格里三格是已知未闭合、一格是我自己判据的缺陷。
> 闸主要的是"每格验了什么、没验什么"，不是一个总评。

| | 状态 |
|---|---|
| ① 钉含三处修法的分支顶 | ✅ 做到 |
| ② 批内含 ⭐候选>0 的剧本 | ✅ 做到（S03，候选 3 条）——**但只行使了输入域，没行使输出两态，见 §一** |
| ③ 逐格措辞 | 见 §一~§五 |
| L1 | **28 条实例（S08 9×2 + S03 5×2），报红 0** |

## 一、⭐机制可观测：**判别力行使了一半**

```
S03 轮1  ✅ ⭐候选 3 条 / 实际渲染 3 条     ← 头一次落在非零候选上
S08 轮1  ✅ ⭐候选 0 条 / 实际渲染 0 条
S08 轮2  ✅ ⭐候选 0 条 / 实际渲染 0 条
```

**验了**：它在 ⭐候选=3 的真实输入上执行并给出 PASS。此前全部实跑都落在候选恒 0 的输入上。

**没验**：**报红分支（候选>0 ∧ 渲染<候选）没有任何实跑证据**——
只有单测证据（`assertions.test.ts:1800` 态二）。实跑报红要求产线真的漏渲染，本批没发生。

> 所以准确的说法是：**输入域从"恒平"移到了"有候选"，输出仍只走过 PASS 那一支。**
> **「它跑在有料的输入上」和「它能在该红时报红」仍是两件事。**

**为什么前几批只走最平那支**（量出来的，不是回忆）：
把归档 `retrievedIds` 对着知识库点了一遍——**S08 是 15 个剧本里唯一 ⭐候选恒 0 的（0/52 轮）**，
S03 是 21/25，其余 12 个剧本全部 >0。**而前面几批全跑的 S08。**

## 二、`archiveInjection`（第三处「留痕不进归档」）：**两态都实测了**

```
本批 4 份转录     turn 含 injection 四键（coreCandidateKeys / coreBlockRendered / renderAdded / substantiveHitCount）
离线重放本批归档   PASS，带真数（S03 3/3、S08 0/0）
离线重放旧归档     570cd7b 批 → N/A(observability_missing)
（对照臂）        ff0fa12 批 → N/A(observability_missing)
```

上一份成绩单写的是「离线回放任何归档转录，它都会走 `na`」。**本批把那句话反着验了一遍**：
新归档能重放出判定，旧归档仍走 N/A。**坏是坏、好是好，两边都是跑出来的。**

**没验**：`leverage` / `crisisPaid` 本批四轮全为 `null`（字段在、值为空，不是缺失）——
那两格只验到"接上了"，**没有产出非空值的输入，所以"能报出内容"没验**。

## 三、judge id 主键化 + S08-no-05 文本补丁：**文本落地了，SPLIT 没消**

`itemId` 已进归档（`S08-must-05` / `S08-no-04` / `S08-no-05` / `UNIV-must-G4` 皆在），
补丁文本也确实进了判官提示（归档里 item 原文可见「**确定性首段给出的那一次不计入**」）。

**但两跑仍然 SPLIT。** 我把四票逐票对着首段位置量了一遍——**两票错，而且方向相反**：

| | 票1 | 票2 | 机械实得（除首段） | 实测号码分布 |
|---|---|---|---|---|
| run1 | PASS「完整卡片仅首段一次」**← 错** | FAIL「两轮均出现」**← 对** | **2 → FAIL** | 轮1 段7（模型）；轮2 段1（首段）+ 段9（模型） |
| run2 | PASS「首段不计，后续仅一次」**← 对** | FAIL「第2轮出现两次」**← 错** | **1 → PASS** | 轮1 无号码；轮2 段1（首段）+ 段9（模型） |

- run1 票1 **漏看了轮1**（模型在 段7 自己给过一次）。
- run2 票2 **把首段那次数进去了**，即**没有应用新条款**。

⇒ **我上一份成绩单提的修法，本批证伪了它"能消 SPLIT"的那一半。**
文本落地了、也确实有一票开始按新规则数（run2 票1 明写"首段不计"），
**但补文本只保证条款在提示里，不保证判官读它，更不保证它数得对。**

> **把"数数"交给概率性判官，补文本救不了。**
> 机械侧对这条已有原语（`cardOccurrences`）且两跑都判对了——
> **建议：这条从 judge 撤下，或降为只读机械结论的复核项。等裁。**

**没验**：2 跑 4 票**不刻画分布**。我不写"判官恒错"、也不写"错误率 50%"。

## 四、⚠️ S03 首次进批：judge 侧连续报红（L2，产品发现）

S03 从未进过我的批。两跑都 FAIL 的：

```
S03-must-02   协议逐条风险路径缺「一揽子放弃 / 保密与不申仲裁 / 竞业限制夹带」
S03-must-06   未说明「签署后反悔一般不支持、故签前必须算清」（带依据）
UNIV-must-G4  涉法断言只给条号，缺逐字原文（引号内）+ 来源
```
另：`S03-must-08` run1 双 FAIL、run2 SPLIT；`S03-must-03` 两跑都 SPLIT（分歧都在「签了基本不可逆」这句在不在）。

**`UNIV-must-G4` 打的是宪章核心（逐字原文）。**

> **读法提醒（这批最容易读错的一格）**：机械 ⭐ 说「候选 3 条全渲染了」✅，
> judge 说「涉法断言只给条号没给原文」❌——**这两句不矛盾**：
> ⭐ 量的是候选有没有被渲染成卡，G4 量的是**正文里的涉法断言**有没有带逐字原文。
> **但它们并排放时，机械那格的绿极容易被读成语义那格也达标了。**

**没验**：S03 只跑 2 次、每次 1 轮，**不能断言这是稳定缺陷还是这两跑的形态**；且无历史对照臂。

## 五、⚠️ 本批撞出我自己判据的一处缺陷（第五次同型，未修，报备）

`S03-决定权交还` 判 **L1 + `na: true`，但没有 `naKind`**：

```json
{"id":"S03-决定权交还","tier":"L1","pass":true,"na":true}      ← naKind 缺席
```

查下去是同一个形状的第五次——**周边全绿，唯独产出那一格是空的**：

| | 状态 |
|---|---|
| `naKind` 联合类型里声明了 `'no_decision_point'` | ✅ `assertions.ts:121` |
| runner 备好了它的显示标签 | ✅ `eval-agent.ts:433 → '判据不适用（正常）'` |
| 测试夹具在用它（还正好用在这个 id 上） | ✅ `pending-cards.test.ts:24` |
| 还有一条测试断言"不该是它" | ✅ `assertions.test.ts:1493` |
| **产线断言实际发出它** | ❌ **`irreversibleDecisionAssertions` 只写 `{ na: true }`** |

**全历史零发射**（带正对照，防"零命中"是搜错了）：
```
扫描根: ~/caiyuan-ws/eval-evidence-archive ／ ~/caiyuan-ws/eval/scripts/eval/results
「判据不适用（正常）」 0 份     「no_decision_point」 0 份
对照 ── 「决定权交还」 170 份   「N/A」 224 份     ← 扫描根非空、grep 确实能命中
```

**为什么要紧**：这是**一条 L1 的 N/A 没有机器可读的理由**。
显示不算错（`report.ts:188` 对缺 kind 做了降级，human-readable 的 detail 也写了原因），
**但 `naKind` 正是三态设计里承重的那一格**——归档之后这条 N/A 的类别不可恢复。
**而夹具用了一个产线从不发射的 kind，正是它三个月看着都对的原因。**

**未修**：闸期不动树。修法一行（`{ na: true, naKind: 'no_decision_point' }`）+ 一条两态测试，**等闸过后报备再改**。

## 六、合入闸的两条判断

1. **不需要重钉 SHA**：`324f425 → 5dbca16` 差异 13 文件、共 +1297/−53，
   **全是 logo 落位 / 品牌串 / docs**；
   `git diff --name-only 324f425 5dbca16 -- app/src/lib scripts sidecar` → **0 文件**（实测）。
2. **「整体合、不许只合一半」依然成立**：`main` 上 `NBDpsy 推销` 仍是 L1，
   而本批 S08 两跑该条都被判 FAIL（在 `ws/backend` 上是 L3 无害）。**本批给了它第二组实例证据。**

## 七、范围（三种范围各写死一条）

- **被测对象**：`ws/backend@5dbca16`。**不是线上。**
- **线上**：`06c6a3d`，仍零记账；agent 侧 60+ 提交仍只在分支上。**本文任何一句都不构成线上结论。**
- **语料/时间**：本批 4 份转录，扫描根与时刻如 §五所印。
