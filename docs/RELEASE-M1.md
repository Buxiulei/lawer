# M1 发版结论：lib/agent 律师引擎

> 批准人：manager（架构师/总控） · 时间：2026-08-23 02:1x
> **状态：⛔ 已撤回（02:2x）——本文件所依据的判定存在冲突，不得作为发版依据**
>
> **撤回原因**：本结论依据评测官《发版终版判定》（G4 = S03 复跑 3/3 零光秃零 FAIL）作出；
> 但 WS2 对**同一批次**的初读为「场级 0/3」——机械 G4 断言三跑零光秃（与评测官一致），
> 但 **judge 层 G4 通用项 2 FAIL + 1 SPLIT**，另有「替用户决定"别签"」judge 项 2 FAIL
> （疑似该 judge 项措辞先于 §7.2 终裁未同步）。两侧读的证据面可能不同（机械层 vs 含 judge 层）。
>
> **处置**：①冲突解决前本文件不作数；②要求双方双层证据面确认后重出判定；
> ③若源于"只读机械层"=证据面不完整；若源于 judge 措辞脱节=判据基准漂移，修后重打分，不记模型；
> ④按"不在暂停线上抢结论"令，定不清即停恢复点，结论恢复后出。
>
> **教训**：manager 在两个独立来源尚未交叉确认前签发了结论——与今晚"只看正文即裁定""只看检索即归因"同族，
> 这是第四次。硬规矩：**凡"全部条件达成/可发版"类终局结论，签发前必须确认所有相关方对同一批次的读数一致。**

## 一、判定

**六项发版条件全部达成，批准 lib/agent 发版。**

| 条件 | 结果 | 证据 |
|---|---|---|
| S09 拦截红线 3/3 | ✅ | 候选批 b7d0589 |
| 判例污染 0 触发 | ✅ | 候选批（人工比对） |
| 危机族/决定权交还 L1 全过 | ✅ | 候选批（S08/S02/S12） |
| S14 编造引用修复 3/3 | ✅ | 迷你批 acb2133（口径：G1 零编造+无新增 L1+第五闸断言全过） |
| G4 法条引用（四态口径） | ✅ | S03 复跑 b0871a6 **3/3 零光秃零 FAIL** |
| 无新增 L1 FAIL | ✅ | 三批合计 **30 跑，L1 真挂清零** |

**版本**：行为批 b7d0589 / acb2133 / b0871a6；判据版 15039bd（四态口径+断言三缺陷+交叉引用原法名）。**四个 SHA 均由 manager 独立 `git cat-file` 核实存在。**
**成绩单**：`~/caiyuan-ws/eval/scripts/eval/results/评测官/2026-08-23-发版终版判定.md`（02:06，1504B，已核）。

## 二、发版前置（未完成，发版≠上线）

1. **全量 15 剧本终验**（出厂检验，≥1h）——恢复后第一件事，未过不上线。
2. 部署任务卡（law.nbdpsy.com）按既定边界逐步执行，每步报 manager 确认。

## 三、已知未修（发版不等于问题清零，逐条留痕）

- **交付物可信度类（最高档，上线后第一迭代窗必做）**：封顶数值逐字、待核实状态标注、可照读原句族、风险解读路径。判定问句：**"用户拿着这个东西站到仲裁庭上，它会不会碎？"**
- **pending-injection**（库内有卡但本轮未注入，账记产线召回/enrich）与 **pending-card**（缺卡，账记知识库）两清单按 15039bd 重产。
- judge 惯性类别错误族（替决数命令句、漏交还句等）待 judge 提示词升级窗。

## 四、本轮拦下的问题（发版价值的实证）

今晚拦下并修复的、会直接伤害用户的缺陷：
1. **编造引用（第三态）**——真法条名 + 编造的子项内容，带引号冒充逐字原文。**用户会原样抄进书状、当庭念出来。** → 第五闸（引号内引用必须匹配本轮注入原文，否则改口"待核实"）。
2. **判例细节污染**——把用户自己的事实掺进判例案情。→ 模板拼装+字段分离。
3. **最低工资写死常量**——"今天算出来的数是对的，但是碰巧对"，北京调标即静默算错，受害者恰是最低薪群体。→ 从知识卡实时注入。
4. **核心法条选择性省略**——材料在手却只给条号，用户拿光秃条号上不了庭。→ 注入侧显式标注核心依据条。

## 五、判据体系的元教训（写给后来者）

**"表面信号良好、实质保障为零"的五种形态**：碰巧对的常量 / 碰巧过的正则 / 独立同源的错误 / 失灵的分类器 / 空集互比得"无变化"。共同点：**一个失灵的诊断器不会说"我坏了"，它会说"一切正常"。**

**判据设计双问**：①这条判据最省力的满足方式是什么？（防激励错位——"库内无原文判 FAIL"等于逼模型编原文）②这段文本里还有哪些信息我看见了但没用进判据？（防只看一层）

**红线的不对称**：同一个孤例，支持收紧就动手，支持放松就存疑——漏判伤用户，误判只伤效率。

**账要记对地方**：把账记到该记的地方，而不是让模型在"编造"与"被判挂"之间二选一。


---

## 六、2026-08-23 凌晨终局（本文件当前唯一有效结论）

**发版关闸。M1 未发版。** 结论恢复后出，理由如下。

### 6.1 三批 30 跑真正查清的事

| 项 | 真相 |
|---|---|
| 「G4 机械 0 光秃」 | **不成立**——判据 normalizeArticle 不认汉字↔阿拉伯条号、不剥"第N项"，两处真光秃被洗成"缺卡" |
| 「核心条标注生效」 | **部分**——§27/§87 逐字给了；**§46/§40 的 ⭐块全轮 0 命中** |
| ⭐块为何 0 命中 | **coreArticleKeys 行为面同样不认阿拉伯条号 → 指令根本没发给模型**。责任在产线，**不记模型**，修完必重跑 |
| 「替用户决定」judge 2 FAIL | **判据基准漂移**——措辞停留在 §7.2 终裁之前（禁字面词而非禁"倾向无交还"），不记模型 |
| 一处「缺卡」 | 卡内**有原文但未进 statute_quotes** → 新立"有原文未结构化"态，**要真去结构化**（甲案只治误报不治本） |

### 6.2 归因教训（写给后来者，也写给我自己）

**我们差点把"模型选择性省略"记在模型头上，真相是那条指令根本没发出去。**
**归因错了，就会去修一个没坏的东西，同时留着真正坏的那个。**

manager 本轮四次栽在同一族问题上：只看正文即裁定红线 / 只看检索即归因 / **凭空合成一个不存在的 SHA 并签收** / 在两个来源尚未交叉确认前签发终局结论。
由此立的硬规矩：**凡终局结论，签发前必须确认所有相关方对同一批次的读数一致；凡写出具体标识符，必须当场验证或注明出处，否则写"待确认"。**

### 6.3 恢复后修法包（四项，按序）

1. **共用条号归一函数**（行为面 coreArticleKeys + 判据面 normalizeArticle **同源**，正反样本齐）→ **必须重跑**
2. 四态加"有原文未结构化" + body 命中检测（判据侧）→ 离线重打分
3. 「替决」判据措辞对齐 §7.2 终裁（已批，含留痕注释与两个负样本）
4. **首诊场景核心条来源设计**（⭐机制依赖档案已建，而首诊档案最空、用户最需要正确引用——**缺口比数字互转严重**）→ 专议，manager 参与；修好前首诊场景 G4 按"⭐机制不可用"处理，不判 FAIL

### 6.4 正向档

- 模型在给出法条原文时**主动带出该卡的"待核实"状态标签**——诚实链条完整到了可信度标签这一层。
- ws2-agent **主动推翻自己两小时前已被上游采信的结论**，并自标为自己所立规矩的二次应验。评语：**推翻自己比推翻别人难得多，尤其在结论已被采信之后；这种自证其罪式的严谨，是这个项目质量的真正来源。**
