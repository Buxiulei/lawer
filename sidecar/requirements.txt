# lawer sidecar 依赖
# 用法: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# HTTP 服务
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# FastAPI 的 UploadFile / Form 依赖（缺了会在启动时报错）
python-multipart>=0.0.9

# PAdES 数字签名 + RFC3161 时间戳 + 验签（pyHanko — 基于 C/OpenSSL 的高性能库）
pyhanko>=0.34.0
# pyHanko 依赖的加密库
cryptography>=42.0.0
# 直接使用：TSTInfo 解析、信任锚 PEM 解码
asn1crypto>=1.5.1

# 《存证证明》PDF 渲染（中文字体）
reportlab>=4.0

# OCR / ASR（阿里云百炼）
dashscope>=1.20.0

# LTV 吊销信息抓取、转写结果下载
requests>=2.31.0

# 测试
pytest>=8.0
httpx>=0.27
