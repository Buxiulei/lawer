# app/.env.example — 复制为 .env.local 后填真值；真值绝不入库（spec D11）

# 字段级加密主密钥：32 字节，hex(64 字符) 或 base64(44 字符)
# 生成：node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
# 轮换会使既有密文与 *_hash 查找列全部失效，务必长期固定并异地备份。
LAWER_DATA_KEY=
