# deploy/.env 样例 —— 复制为 deploy/.env 并填真值（.env 已被 .gitignore 忽略，禁止入库）
# 这里只放编排本身要用的变量；web 与 sidecar 各自的密钥分别在 app/.env、sidecar/.env。

# 对外域名（Caddy 据此自动签发 HTTPS 证书；须已解析到本机公网 IP）
LAWER_DOMAIN=lawer.example.com

# ACME 证书联系邮箱（Let's Encrypt 到期提醒）
LAWER_ACME_EMAIL=admin@example.com
